# ========== 빌드 산출물 ==========
# Docker 이미지에 포함할 필요가 없는 빌드 결과물들
# (이미 Dockerfile에서 소스 코드를 빌드하므로 중복 제거)

target/          # Maven 빌드 결과물 디렉토리 (컴파일된 클래스, JAR 파일 등)
build/           # Gradle 빌드 결과물 디렉토리 (컴파일된 클래스, JAR 파일 등)
.gradle/         # Gradle 캐시 디렉토리 (빌드 성능 향상을 위한 캐시, 이미지에 불필요)
out/             # IntelliJ IDEA 컴파일 출력 디렉토리
bin/             # Eclipse 컴파일 출력 디렉토리

# ========== IDE (통합 개발 환경) 파일 ==========
# 개발 환경 설정 파일들 (Docker 이미지 실행에 불필요)

.idea/           # IntelliJ IDEA 프로젝트 설정 디렉토리
.vscode/         # Visual Studio Code 워크스페이스 설정 디렉토리
*.iml            # IntelliJ 모듈 설정 파일
*.ipr            # IntelliJ 프로젝트 설정 파일
*.iws            # IntelliJ 워크스페이스 설정 파일
.classpath       # Eclipse 클래스패스 설정 파일
.project         # Eclipse 프로젝트 설정 파일
.settings/       # Eclipse 워크스페이스 설정 디렉토리

# ========== 버전 관리 파일 ==========
# Git 관련 파일들 (Docker 이미지에 포함할 필요 없음)

.git/            # Git 저장소 메타데이터 디렉토리 (버전 히스토리, 브랜치 정보 등)
.gitignore       # Git 무시 파일 목록 (Docker 빌드 컨텍스트에 불필요)
.gitattributes   # Git 속성 설정 파일 (줄바꿈 문자 처리 등)

# ========== 문서 파일 ==========
# 프로젝트 문서 파일들 (이미지 크기 증가 방지 및 불필요한 파일 제외)

README.md        # 프로젝트 설명 문서
LICENSE          # 라이선스 파일
*.md             # 모든 마크다운 문서 파일

# ========== Docker 관련 파일 ==========
# Docker 설정 파일들 (이미지 빌드에 사용되지만 이미지 내부에는 불필요)

Dockerfile       # Docker 이미지 빌드 설정 파일 (이미 사용됨)
.dockerignore    # Docker 빌드 컨텍스트 제외 파일 목록 (이미 사용됨)
docker-compose.yml  # Docker Compose 설정 파일 (로컬 개발용, 이미지에 불필요)

# ========== 로그 파일 ==========
# 애플리케이션 실행 중 생성되는 로그 파일들 (이미지 크기 증가 방지)

*.log            # 모든 로그 파일 (애플리케이션 실행 로그, 에러 로그 등)
logs/            # 로그 디렉토리 (로그 파일들이 저장되는 폴더)

# ========== 운영체제별 파일 ==========
# 운영체제에서 자동 생성되는 메타데이터 파일들

.DS_Store        # macOS Finder 메타데이터 파일 (폴더 뷰 설정 정보)
Thumbs.db        # Windows 썸네일 캐시 파일 (이미지 미리보기 정보)

# ========== 환경 변수 파일 ==========
# 로컬 개발 환경 설정 파일들 (보안상 이미지에 포함되면 안 됨)

.env             # 환경 변수 파일 (데이터베이스 연결 정보, API 키 등)
.env.local       # 로컬 개발 환경 변수 파일
.env.*.local     # 로컬 환경별 환경 변수 파일 (예: .env.development.local)

# ========== 테스트 파일 ==========
# 테스트 코드 및 리소스 (프로덕션 이미지에 불필요, 이미지 크기 감소)

src/test/        # 테스트 소스 코드 디렉토리 (JUnit 테스트, 통합 테스트 등)
