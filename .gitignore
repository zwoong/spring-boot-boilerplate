# ========== 빌드 도구 관련 ==========

# Gradle 빌드 시스템 관련 파일/디렉토리
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Maven 빌드 시스템 관련 파일/디렉토리 (레거시 프로젝트 지원)
target/           # Maven 빌드 결과물 디렉토리
!.mvn/wrapper/maven-wrapper.jar  # 예외: Maven Wrapper JAR 파일은 버전 관리 필요

# ========== IDE (통합 개발 환경) 관련 ==========

# IntelliJ IDEA 관련 파일
.idea/            # IntelliJ IDEA 프로젝트 설정 디렉토리 (워크스페이스 설정, 실행 구성 등)
*.iws             # IntelliJ 워크스페이스 설정 파일 (개인 설정 포함)
*.iml             # IntelliJ 모듈 설정 파일 (프로젝트 구조 정보)
*.ipr             # IntelliJ 프로젝트 설정 파일 (프로젝트 레벨 설정)
out/              # IntelliJ 컴파일 출력 디렉토리

# Eclipse / Spring Tool Suite (STS) 관련 파일
.apt_generated    # 어노테이션 프로세서 생성 파일 (APT: Annotation Processing Tool)
.classpath        # Eclipse 클래스패스 설정 파일
.factorypath      # JPA 엔티티 팩토리 경로 설정 파일
.project          # Eclipse 프로젝트 설정 파일
.settings/        # Eclipse 워크스페이스 설정 디렉토리
.springBeans      # Spring Beans 설정 캐시 파일
.sts4-cache       # Spring Tool Suite 4 캐시 디렉토리
bin/              # Eclipse 컴파일 출력 디렉토리

# Visual Studio Code 관련 파일
.vscode/          # VS Code 워크스페이스 설정 디렉토리 (확장 프로그램 설정, 디버그 구성 등)
*.code-workspace  # VS Code 멀티 루트 워크스페이스 설정 파일
.history/         # VS Code 로컬 히스토리 디렉토리

# NetBeans 관련 파일
/nbproject/private/  # NetBeans 개인 프로젝트 설정 디렉토리
/nbbuild/           # NetBeans 빌드 출력 디렉토리
/dist/               # NetBeans 배포 파일 디렉토리
/nbdist/             # NetBeans 배포 아티팩트 디렉토리
/.nb-gradle/         # NetBeans Gradle 통합 캐시 디렉토리

# ========== 보안 관련 (중요: 절대 커밋하지 말 것!) ==========

# 키 및 비밀 정보 파일 (보안상 Git에 포함되면 안 되는 파일들)
secrets.properties        # 비밀 정보가 포함된 프로퍼티 파일 (API 키, 비밀번호 등)
application-secret.yml   # Spring Boot 비밀 설정 파일 (프로덕션 비밀 정보 포함)
*.key                    # 모든 키 파일 (개인 키, 인증 키 등)
*.pem                    # PEM 형식 인증서/키 파일 (SSL 인증서, 개인 키 등)

# ========== 운영체제별 파일 ==========

# 운영체제에서 자동 생성되는 파일들
.DS_Store         # macOS Finder 메타데이터 파일 (폴더 뷰 설정 정보)
Thumbs.db         # Windows 썸네일 캐시 파일 (이미지 미리보기 정보)

# ========== 로그 파일 ==========

# 애플리케이션 로그 파일
*.log             # 모든 로그 파일 (애플리케이션 실행 로그, 에러 로그 등)
logs/             # 로그 디렉토리 (로그 파일들이 저장되는 폴더)

# ========== 애플리케이션 설정 파일 ==========

# 로컬 개발 환경 설정 파일 (보안상 Git에 포함되면 안 됨)
application-local.yml
application-local.properties
application-*.local.yml
application-*.local.properties

# ========== 임시 파일 ==========

# 컴파일된 클래스 파일 (build/ 디렉토리에 포함되지만 명시적으로 추가)
*.class

# JAR 파일 (빌드 결과물, wrapper 제외)
*.jar
!gradle/wrapper/gradle-wrapper.jar
!**/gradle-wrapper.jar

# 임시 파일
*.tmp
*.bak
*.swp
*~
